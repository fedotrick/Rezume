# Итоговый Проект Часть 2: Триггеры, Представления и Оптимизированные Запросы

**Система управления портфелями ценных бумаг — Часть 2**

Этот проект содержит полный набор триггеров баз данных, представлений (views) и оптимизированных SQL-запросов для расширенного анализа портфелей ценных бумаг.

## 📋 Содержание

### Основные компоненты

1. **3 Триггера** — Для аудита, валидации и обновления
2. **4 Представления** — Для анализа портфелей и ценных бумаг
3. **5 Оптимизированных Запросов** — С использованием продвинутых техник SQL
4. **Примеры использования** — С результатами и рекомендациями
5. **Документация** — Полное описание всех компонентов

## 📁 Структура Проекта

```
final-project-part2-triggers-views-queries/
├── README.md                                      # Этот файл
├── scripts/
│   ├── final_project_part2_triggers.sql           # Все триггеры
│   ├── final_project_part2_views.sql              # Все представления
│   ├── final_project_part2_optimized_queries.sql  # Оптимизированные запросы
│   └── final_project_part2_examples.sql           # Примеры использования
└── documentation/
    └── FINAL_PROJECT_PART2_TRIGGERS_VIEWS_QUERIES.md  # Подробная документация
```

## 🚀 Быстрый Старт

### Предварительные требования

- SQL Server 2017 или выше
- Развернутая **Part 1** Final Project (схема БД и процедуры)
- Права администратора или CREATE DATABASE

### Порядок выполнения скриптов

1. **Создание триггеров:**
   ```sql
   -- Выполнить в SQL Server Management Studio
   USE YourDatabaseName;
   GO
   
   -- Выполнить скрипт
   :r 'scripts/final_project_part2_triggers.sql'
   GO
   ```

2. **Создание представлений:**
   ```sql
   :r 'scripts/final_project_part2_views.sql'
   GO
   ```

3. **Создание оптимизированных запросов:**
   ```sql
   :r 'scripts/final_project_part2_optimized_queries.sql'
   GO
   ```

4. **Запуск примеров:**
   ```sql
   :r 'scripts/final_project_part2_examples.sql'
   GO
   ```

## 🔍 Краткое описание компонентов

### Триггеры (Triggers)

#### 1. `trg_Transactions_Audit`
- **Тип:** AFTER INSERT, UPDATE, DELETE
- **Назначение:** Логирование всех изменений транзакций
- **Результат:** Запись в Audit_Log с полной историей

**Пример использования:**
```sql
-- Вставка записывается автоматически
INSERT INTO dbo.Transactions (PortfolioID, SecurityID, Quantity, Price, Type)
VALUES (1, 1, 100, 150.50, N'BUY');

-- Проверка логирования
SELECT * FROM dbo.Audit_Log WHERE TableName = 'Transactions' ORDER BY LogID DESC;
```

#### 2. `trg_UpdatePortfolioValue_OnQuoteChange`
- **Тип:** AFTER INSERT, UPDATE на Quotes
- **Назначение:** Автоматическое уведомление об изменении котировок
- **Результат:** Логирование обновленных портфелей

#### 3. `trg_ValidateTransaction`
- **Тип:** INSTEAD OF INSERT
- **Назначение:** Валидация данных перед вставкой
- **Проверки:**
  - Портфель существует
  - Ценная бумага существует
  - Quantity > 0
  - Price > 0
  - Достаточное количество для SELL

**Пример использования:**
```sql
-- Успешная вставка
INSERT INTO dbo.Transactions (PortfolioID, SecurityID, Quantity, Price, Type)
VALUES (1, 1, 100, 150.50, N'BUY');  -- ✓ Успех

-- Ошибка валидации
INSERT INTO dbo.Transactions (PortfolioID, SecurityID, Quantity, Price, Type)
VALUES (9999, 1, 100, 150.50, N'BUY');  -- ✗ Ошибка: Portfolio not found
```

### Представления (Views)

#### 1. `vw_PortfolioSummary`
Краткий обзор портфелей с ключевыми метриками

```sql
SELECT * FROM dbo.vw_PortfolioSummary ORDER BY TotalValue DESC;
```

**Поля:** PortfolioID, PortfolioName, Owner, TotalValue, SecurityCount, PositionCount, LastUpdate

#### 2. `vw_PortfolioComposition`
Детальный состав портфеля с процентным распределением

```sql
SELECT Ticker, Name, Quantity, CurrentPrice, TotalValue, Percentage
FROM dbo.vw_PortfolioComposition
WHERE PortfolioID = 1
ORDER BY TotalValue DESC;
```

**Поля:** PortfolioID, SecurityID, Ticker, Name, Type, CurrentPrice, Quantity, TotalValue, Percentage

#### 3. `vw_PortfolioPerformance`
Производительность портфеля с расчетом ROI

```sql
SELECT TOP 10 *
FROM dbo.vw_PortfolioPerformance
WHERE TotalInvestment > 0
ORDER BY ReturnPercentage DESC;
```

**Поля:** PortfolioID, PortfolioName, Owner, CurrentValue, TotalInvestment, ProfitLoss, ReturnPercentage

#### 4. `vw_SecurityRanking`
Ранжирование ценных бумаг по активности

```sql
SELECT TOP 10 *
FROM dbo.vw_SecurityRanking
WHERE TradeCount > 0
ORDER BY TradeCount DESC;
```

**Поля:** SecurityID, Ticker, Name, Type, Sector, AvgPrice, MinPrice, MaxPrice, TradeCount, PortfoliosContaining

### Оптимизированные Запросы

#### 1. Moving Average (Скользящее среднее)
Анализ тренда цен с 7-дневным и 30-дневным скользящими средними

```sql
SELECT TOP 30 *
FROM dbo.vw_SecurityMovingAverage
WHERE Ticker = 'AAPL'
ORDER BY QuoteDate DESC;
```

**Используемые техники:** ROW_NUMBER(), LAG(), AVG() OVER()

#### 2. Portfolio Hierarchy (CTE)
Иерархический анализ портфелей с многоуровневой агрегацией

```sql
SELECT *
FROM dbo.vw_PortfolioTransactionHierarchy
ORDER BY ROI_Percentage DESC;
```

**Используемые техники:** Множественные CTE, условные агрегации

#### 3. Complete Portfolio Info (JOINы)
Полная информация о портфеле со всеми связанными данными

```sql
SELECT *
FROM dbo.vw_CompletePortfolioInfo
WHERE PortfolioID = 1
ORDER BY TransactionDate DESC;
```

**Используемые техники:** Множественные LEFT JOINы, подзапросы

#### 4. Batch Processing (Обработка больших данных)
Эффективная обработка 100k+ транзакций

```sql
EXEC dbo.sp_BatchProcessTransactions 
    @BatchSize = 5000,
    @MaxBatches = 50;
```

**Используемые техники:** ROW_NUMBER(), временные таблицы, цикл обработки

#### 5. TOP Portfolios by ROI (Ранжирование)
Выявление лучших портфелей по доходности

```sql
SELECT *
FROM dbo.vw_TopPortfoliosByROI
ORDER BY ROI_Rank;
```

**Используемые техники:** Вложенные подзапросы, ROW_NUMBER()

## 📊 Примеры Результатов

### Portfolio Summary
```
PortfolioID | PortfolioName    | Owner      | TotalValue | SecurityCount | PositionCount
------------|-----------------|------------|------------|----------------|---------------
1           | Value Stocks     | Bob Jones  | 24177.50   | 5             | 15
2           | Growth Fund      | Alice Smith| 18950.00   | 4             | 12
3           | Income Stream    | Charlie B. | 15432.75   | 3             | 8
```

### Portfolio Performance (Top 5)
```
PortfolioID | PortfolioName  | CurrentValue | TotalInvestment | ReturnPercentage
------------|----------------|--------------|-----------------|------------------
5           | Tech Growth    | 145000.00    | 100000.00       | 45.00
3           | Balanced Port. | 132500.00    | 100000.00       | 32.50
1           | Value Stocks   | 128900.00    | 100000.00       | 28.90
```

### Security Ranking (Top 5)
```
Ticker | Name              | Type  | TradeCount | PortfoliosContaining | AvgPrice
-------|-------------------|-------|------------|----------------------|---------
AAPL   | Apple Inc.        | Stock | 156        | 12                   | 172.45
MSFT   | Microsoft Corp    | Stock | 143        | 11                   | 305.80
GOOGL  | Alphabet Inc.     | Stock | 128        | 10                   | 138.90
```

## ⚡ Оптимизация Производительности

### Рекомендуемые индексы

```sql
-- Для Moving Average
CREATE NONCLUSTERED INDEX IX_Quotes_SecurityID_QuoteDate_Price
ON dbo.Quotes (SecurityID, QuoteDate DESC, Price)
INCLUDE (Volume);

-- Для ROI расчетов
CREATE NONCLUSTERED INDEX IX_Transactions_Type_PortfolioID
ON dbo.Transactions (Type, PortfolioID)
INCLUDE (Quantity, Price);

-- Для аудита
CREATE NONCLUSTERED INDEX IX_AuditLog_Action_ChangeDate
ON dbo.Audit_Log (Action, ChangeDate DESC);
```

### Типичные времена выполнения

| Операция | Время (100k записей) |
|----------|----------------------|
| vw_PortfolioSummary | < 100 ms |
| vw_PortfolioComposition | 200-500 ms |
| vw_SecurityMovingAverage | 300-800 ms |
| sp_BatchProcessTransactions | ~15-30 sec |

## 🔧 Использование с Part 1

Часть 2 полностью совместима с Part 1 Final Project:

- **Триггеры** срабатывают при вставке через `sp_AddTransaction`
- **Представления** автоматически отражают изменения
- **Audit_Log** заполняется триггерами и процедурами

Пример интеграции:
```sql
-- Part 1 процедура
DECLARE @Result NVARCHAR(20), @TxID BIGINT;
EXEC dbo.sp_AddTransaction
    @PortfolioID = 1,
    @SecurityID = 1,
    @Quantity = 100,
    @Price = 150.50,
    @Type = N'BUY',
    @Result = @Result OUTPUT,
    @TransactionID = @TxID OUTPUT;

-- Триггер автоматически логирует в Audit_Log
-- Представления отражают новые данные
```

## 📝 Документация

Подробная документация находится в:
- **`documentation/FINAL_PROJECT_PART2_TRIGGERS_VIEWS_QUERIES.md`** — Полное описание всех компонентов, формулы, примеры

## 💡 Часто Задаваемые Вопросы

**Q: Почему триггер trg_ValidateTransaction использует INSTEAD OF?**
A: Это позволяет полностью контролировать процесс и отклонять невалидные данные до вставки.

**Q: Как часто обновляются представления?**
A: Представления обновляются в реал-тайм при изменении базовых данных.

**Q: Можно ли использовать эти запросы с разными БД?**
A: Да, нужно только изменить имя БД в скрипте выполнения.

**Q: Какой максимальный размер данных поддерживает batch-обработка?**
A: Может обработать миллионы записей, обрабатывая их батчами по 10k строк.

## 🐛 Известные Ограничения

1. Триггер валидации требует наличия всех четырех колонок (PortfolioID, SecurityID, Quantity, Price)
2. Moving Average требует минимум 7 точек данных для точного расчета
3. Batch-обработка создает временные таблицы (требует достаточно дискового пространства tempdb)

## ✅ Проверочный Список

- [ ] Part 1 развернут и работает
- [ ] SQL Server версии 2017+ установлен
- [ ] Выполнены все три скрипта (triggers, views, queries)
- [ ] Примеры (examples.sql) запущены успешно
- [ ] Индексы рекомендации добавлены (опционально)

## 🎯 Следующие Шаги

1. Добавить рекомендуемые индексы для оптимизации
2. Интегрировать с приложением (C#, Python и т.д.)
3. Настроить мониторинг производительности
4. Рассмотреть архивирование старых логов Audit_Log
5. Добавить дополнительные триггеры для других таблиц при необходимости

## 📞 Поддержка

При возникновении проблем:
1. Проверьте версию SQL Server (требуется 2017+)
2. Убедитесь, что Part 1 полностью развернут
3. Проверьте права доступа пользователя
4. Просмотрите логи ошибок в SQL Server

## 📚 Ресурсы

- [Microsoft SQL Server Documentation](https://docs.microsoft.com/en-us/sql/)
- [Triggers Best Practices](https://docs.microsoft.com/en-us/sql/t-sql/statements/create-trigger-transact-sql)
- [Window Functions](https://docs.microsoft.com/en-us/sql/t-sql/functions/window-functions-transact-sql)
- [Common Table Expressions (CTE)](https://docs.microsoft.com/en-us/sql/t-sql/queries/with-common-table-expression-transact-sql)

## 📄 Лицензия

Этот проект является учебным материалом.

---

**Версия:** 1.0  
**Дата создания:** 2024-05-15  
**SQL Server:** 2017+  
**Статус:** Полностью функционален ✓
